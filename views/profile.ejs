<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-[#020251] min-h-screen flex items-center justify-center text-white">
    <div class="bg-white text-black rounded-lg shadow-lg p-8 w-96">
        <div class="bg-blue-600 text-white text-center py-4 rounded-t-lg">
            <h1 class="text-2xl font-bold">Welcome, <%= user.firstName %> <%= user.lastName %></h1>
        </div>
        <div class="mt-6">
            <h2 class="text-lg font-medium mb-2">
                <span class="font-bold">First Name:</span> <%= user.firstName %>
            </h2>
            <h2 class="text-lg font-medium mb-2">
                <span class="font-bold">Last Name:</span> <%= user.lastName %>
            </h2>
            <h2 class="text-lg font-medium">
                <span class="font-bold">Email:</span> <%= user.email %>
            </h2>
        </div>
        <form action="/user/logout" method="GET" class="mt-6">
            <button type="submit"
                class="w-full bg-blue-600 text-white py-2 px-4 rounded-lg hover:bg-blue-700 transition">
                Logout
            </button>
        </form>
    </div>
</body>

</html>
